<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.trip.service.login.LoginMapper">
    <insert id="insertUser" parameterType="com.example.trip.commendVO.JoJoHaVO">
        INSERT INTO tripMember (username, password, email, name, nickname)
        VALUES (#{username}, #{password}, #{email}, #{name}, #{nickname})
    </insert>

</mapper>
